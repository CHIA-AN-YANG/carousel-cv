{
  "name": "carousel-cv-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "bun dist/index.js",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3",
    "axios": "^1.8.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}